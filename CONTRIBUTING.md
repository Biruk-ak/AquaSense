# Contributing to AquaSense

1. Use feature branches off `main`.
2. Keep NestJS modules isolated by domain.
3. Add unit tests for services and domain processors.
4. Run `npm test` before opening a PR.
